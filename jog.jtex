\newcommand{\pref}[1] {
    \aref{ #1 }. fejezet \apageref{ #1 }. oldalon
    }

\newcommand{\tlaw}[3]
{
    #1 & #2 & #3 \\ \hline
}

\begin{longtable}{p{7cm} | p{5cm} | l}

    \textbf{feladat} & megjegyzés              & \textbf{referencia} \\

{% for row in data %}
   \tlaw { {{row.req}}  } { {{ row.comment }} } { 
       {% for ref in row.ref %}
       \pref{ {{ ref }}}
       {% endfor %}
   }
{% endfor %}



\end{longtable}

